第四天
1.授权所有网段用户mydba在所有库所有表的全部权限 并可以对其他用户授权，密码任意。再写出重新加载权限的SQL语句。
grant all on *.* to mydba@"%" identified by '123456' with grant option;
flush privileges;

2.撤销用户mydba的删除权限
revoke delete on *.* mydba@"%";

3.破解MySQL的root密码
 systemctl  stop mysqld.service           //停止服务
 systemctl  status mysqld.service          //确认状态
vim /etc/my.cnf
skip_grant_tables

systemctl  start mysqld.service
mysql  //直接回车即可
mysql> update mysql.user set authentication_string=PASSWORD('123qqq…A') WHERE user='root' AND host='localhost';      //重设root的密码
exit  

#skip_grant_tables=1                              //注释掉或删除此行
systemctl  restart mysqld.service
 mysql -uroot –p123qqq…A

4.mysqldump针对所有库, 指定库和指定表备份的语法格式
mysqldump -uroot -p123456 [-A,库名,库名.表名] > 目录/xxx.sql;

5.什么是binlog日志以及如何开启它
也称作 二进制日志
MYSQL服务日志文件的一种
记录出查询之外的所有SQL命令
可用于数据备份和恢复
配置MYSQL主从同步的必要条件

vim  /etc/my.cnf
[mysqld]
    server_id=1  //指定server_id
log-bin=/mylog/db50  //指定日志目录及名称                           
:wq
[root@dbsvr1 ~]# mkdir  /mylog   //创建目录
[root@dbsvr1 ~]# chmod  mysql  /mylog   //修改所有者
[root@dbsvr1 ~]# systemctl  restart mysqld.service  //重启服务

mysql -uroot -p123qqq...A //管理员登录
mysql> show master status; //查看日志信息

 flush logs;  //刷新日志

